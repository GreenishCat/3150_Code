#ifndef STRING_UTILS_H
#define STRING_UTILS_H

namespace string_utils{
    bool runStringOption();
    /*
    Multi line comment:
    runstringoperation() promopts the user for a string length
    and then copies the specified num of characters into an array,
    manually inserts a null term, and prints the resulting string
    */
}

#endif
